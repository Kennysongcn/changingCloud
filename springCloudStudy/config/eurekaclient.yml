server:
  port: 8763
spring:
  application:
    name: eurekaclient
  rabbitmq:
     port: 5672
     username: guest
     password: gust
     virtualHost: /
     publisherConfirms: true
management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info,bus-refresh
